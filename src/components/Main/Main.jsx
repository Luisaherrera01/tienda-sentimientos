import {Header} from "../header/Header"
import {Productos} from "../main/Productos"

export const Main = () => {
  return (
    <section>
        <section>
            <Header/>
        </section>
        <section>
            <Productos/>
        </section>

    </section>
  )
}

